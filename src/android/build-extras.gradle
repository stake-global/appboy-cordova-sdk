repositories {
    google()
    mavenCentral()
}

dependencies {
    implementation 'com.braze:android-sdk-ui:42.2.0'
    implementation 'com.google.firebase:firebase-messaging:23.0.0'
}

// No postBuildExtras: Capacitor's app template applies google-services itself, and this hook cannot
// load it from a plugin script. It also overwrites any other plugin's hook. See FE-7492.
