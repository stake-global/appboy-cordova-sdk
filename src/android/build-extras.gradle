repositories {
    google()
    mavenCentral()
}

dependencies {
    implementation 'com.braze:android-sdk-ui:42.2.0'
    implementation 'com.google.firebase:firebase-messaging:23.0.0'
}

// Stake custom: upstream's buildscript block and postBuildExtras hook are removed. Capacitor's app
// template already applies google-services, and the hook cannot load it from a plugin script (FE-7492).
